{% extends 'base.html.twig' %}

{% block title %}Liste des Biens!{% endblock %}

{% block body %}

<div class = "jumbotron">
    <article>
            <h2>{{article.title}}</h2>
            <div class = "metadata">Ecrit le  {{article.createdAt | date('d/m/Y')}} a {{article.createdAt | date('h/i')}}/ {{article.categorie.titre}} </div>

            <div class = "content">
                <img src= " {{article.image}} "alt="">
            {{article.content}}
            </div>          
    </article>
    

    <section id="commentaires">
    
    </section>

<div class="container">


{% endblock %}